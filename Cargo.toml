[package]
name = "forgit"
version = "0.1.0"
edition = "2021"
publish = false
description = "forgit (git+fzf) ported to a native zshrs plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
zshrs-plugin = { git = "https://github.com/MenkeTechnologies/zshrs" }
