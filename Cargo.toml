[package]
name = "forgit"
version = "0.1.0"
edition = "2021"
publish = false
description = "forgit (git+fzf) ported to a native zshrs plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
znative = "0.12"
